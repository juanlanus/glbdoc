******* VALSUSCT-FS.v03.0001
******* ESTADISTICO DE TOTALES REPORTADOS POR SUSCRIPTOR Y POR CADA CAMPO
******* DEL MAESTRO RESUMIDO, DURANTE EL PROCESO DE VALIDACION POR EL
******* PROGRAMA CALIFICADOR PARA CUENTAS CORRIENTES
CBIT05*----------------------------------------------------------------*
CBIT05* OBJETIVO    * AUMENTAR A 80 EL NUMERO DE OCCURRENCIAS DE LAS 
CBIT05*             * VARIABLES REPORTADAS.
CBIT05* AUTOR       * CONTACT & BUSINESS IT - CBIT03 
CBIT05* FECHA       * JUNIO DE 2009
CBIT05*------------------------------------------------
      $SET CONSTANT SW-STATUS-VALSUSCT"NO"
           SELECT VALSUSCT
                  ASSIGN TO DISK VALSUSCT-TITLE
                  ORGANIZATION IS INDEXED
                  ACCESS MODE IS DYNAMIC
                  RECORD KEY IS LLAVE-VALSUSCT
      $IF SW-STATUS-VALSUSCT = "NO" 
      $DISPLAY ".............COMPILANDO VALSUSCT SIN STATUS.........."
      $ELSE
      $DISPLAY ".............COMPILANDO VALSUSCT CON STATUS.........."
                  FILE STATUS IS COD-RETORNO-VALSUSCT
      $END
                  LOCK MODE IS MANUAL WITH LOCK ON RECORD.
